# Dependencies
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/releases

# Build output
.next/
out/
dist/
build/
*.tsbuildinfo
next-env.d.ts

# Environment and secrets
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
service-account*.json

# Platform linkage (machine-specific project IDs, not source)
.vercel/

# Test and coverage
coverage/
.nyc_output/

# Editor and OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# deer-flow is a clone of github.com/bytedance/deer-flow kept as a local
# reference checkout. It is upstream, not ours: committing it would embed a
# broken half-copy of another project and collide on every `git pull` there.
deer-flow/
